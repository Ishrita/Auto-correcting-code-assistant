# Auto-correcting-code-assistant
Built a self-correcting code assistant. It is straightforward but effective. Instead of generating code just once, the agent works in a loop where:  The user gives a task. AI writes code. Code gets executed. Errors are captured. AI analyzes the error. AI rewrites the code. Process repeats until success.
